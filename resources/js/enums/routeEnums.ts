export enum AuthRoutesEnum {
    LOGIN = '/login',
    LOGOUT = '/logout',
}

export enum  AppRoutesEnum {
    DASHBOARD = '/',
    SETTINGS = '/settings',
}

export enum ErrorRoutesEnum {
    NOT_FOUND = '/404',
}
